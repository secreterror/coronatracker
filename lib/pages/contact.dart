import 'package:corona/nav/blocnav.dart';
import 'package:flutter/material.dart';
class Contact extends StatelessWidget with NavigationState{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'this is about developer '
        ),
      ),
    );
  }
}
